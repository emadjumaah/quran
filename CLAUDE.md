# مشكاة — المرجع الحاسوبي للمصحف (حلقة القرآن)

تطبيق ويب حيّ على www.mishkat.qa (النطاق المقرر `quran.mishkat.qa`): القارئ، نبراس، ألفاظ التنزيل، بناء المصحف، «الفحص والتحقيق» (ميزان الأقوال + ملفّ جمع القرآن)، ونسخة إنجليزية. **بلا سيرفر** — ملفات ثابتة + دوال حراسة (Vercel، جذره `js/apps/studio/`). أحد حلقات **مجموعة مشكاة** الثلاث — خارطتها في `docs/MISHKAT.md`.

## طريقة العمل — اقرأ هذا أولًا
- **مدير القرآن**: جلسة فابل ٥ مقيمة في workspace القرآن — تملك `findings/MASTER-PLAN.md` (الدفتر الرئيس) و`findings/FAHIS-PLAN.md`، تصدر برومبتات التنفيذ، وتراجع تقارير العودة قبل القبول.
- **جلسات التنفيذ**: أوبوس ٥ ببرومبت مكتوب (`findings/SESSION-*.md`) — نفّذ حدوده فقط وعُد بتقريره. **الأسراب**: سونيت، بأجزاء مقيَّمة ودفاتر شفافية (`findings/SWARMS-REGISTRY.md`).
- **جلسات التنفيذ لا تدفع ولا توسم** — الدفع بعد قبول الإدارة بأمر المالك.
- **المرجع الذي يُقرأ ولا يُمَسّ**: مستودع `history` (مصدر باب «ملفّ جمع القرآن» — لقطات مجمّدة بمانيفست تجزئات، لا قراءة حية).

## بريد المجموعة — لجلسة الإدارة وحدها
- إن كنتَ **مدير القرآن**: أول كل جلسة اقرأ ما استجدّ في `../mishkat/bulletins/` (العقد في `../mishkat/README.md`) وقيّد إيصال كل بلاغ في `docs/GROUP_MAIL.md`. **بلاغ إجراءٍ لا يُغلق إلا بإيصاله.** وبلاغُك للمجموعة يُكتب ملفًا هناك ولا يُنقل عبر المالك.
- إن كنتَ **جلسة تنفيذ**: البريد ليس شأنك — بند برومبتك يكفيك.

## قواعد ثابتة
- العربية **فصيحة** دومًا، ولا لغة أداةٍ في صفحات القارئ (لا أسماء جلسات/نماذج/مسارات داخلية — بوابة خ٧ نموذجًا).
- وسم **منقول/محسوب/مولَّد** في كل معروض؛ درجات معلنة قابلة للنقض؛ «قراءة من المصادر لا فتوى».
- البوابات تُدقَّق هي نفسها بضبط سالب، **ولا `\b` مع نص عربي** (بلاغ `2026-08-12-arabic-word-boundary`).
- البناء: `pnpm build` في `js/apps/studio` — ولا يُنشر شيء قبل معاينة المالك.
