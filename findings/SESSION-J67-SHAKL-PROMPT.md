# ج٦٧ — الشكلُ النهائيّ: القِشرةُ والأثاثُ في جلسةٍ واحدةٍ خفيفة

> جلسة تنفيذ (Opus 5). لا سرب. المستودع: `/Volumes/data/new-projects/quran`.
> **إيداعات محلية على main، ولا دفع.** أصدرتها إدارة القرآن 2026-08-15 **دمجًا لج٦ وج٧ (الأثاث)** لضغط الجلسات بأمر المالك.
> **اقرأ ثلاثةً فقط**: هذا الملفَّ · `findings/SESSION-J6-QISHRA-PROMPT.md` §٠–١ (تشخيصُ القشرة بمواضعه) · `findings/SESSION-J7-ATHATH-MUSHAF-PROMPT.md` §٠–١ (تشخيصُ الأثاث بمواضعه). **وميثاقا الوجه والعبارة مُلزِمان بلا إعادة قراءة — خلاصتُهما: لا حواراتِ متصفّح، أهدافُ لمس ≥٤٤px، لا نصَّ <١٥px، لا لوحَ يطمس القرآن، عربيّةٌ فصيحةٌ بلا لغة أدوات.**

## ⏱ ميثاقُ الوقت — مُلزِم

1. **التشخيصُ جاهزٌ بمواضعه في الملفّين** — اذهب إلى السطر وأصلح، **ولا تُعِد بحثًا**.
2. **عليك الساكنُ وحدَه**: `tsc --noEmit` وفحوصٌ ساكنة. **وبوّاباتُ المتصفّح والبناءُ على الإدارة عند المراجعة — لا تُشغّلها أنت البتّة.**
3. **لا استطراد**: ما ظهر لك يُكتب سطرًا ولا يُبنى.
4. الحدُّ على الهدر لا على العمل: أودِع تباعًا، ومحاولةٌ تكرّرت ثلاثًا على حالها تُقيَّد ويُمضى.
5. التقريرُ جدولٌ واحدٌ موجز.

## البنود (كلُّها تعديلاتُ CSS/JSX موضعيّةٌ مشخَّصة)

**من ج٦ (القشرة):**
1. **توحيدُ أزرار الرأس**: شكلٌ ومقاسٌ ونصفُ قطرٍ واحد للجميع — `theme.css:220` يوحّد زرّين ويترك `.tatabbu-btn` قرصًا (`:2697`) والإعداداتِ في غلاف. **لا استثناء.**
2. **وحدةُ الهويّة**: الشعارُ والاسمُ عنصرٌ واحدٌ على خطِّ أساسٍ واحدٍ بمسافةٍ ثابتة.
3. **الشحوب**: أيقوناتُ القشرة وتسمياتُها **≥٤٫٥:١** وحدودُها ≥٣:١ (اليوم `--panel-2` على `--bg` فرقٌ لا يُرى) — عدِّل الرموزَ في `:root` ومشتقّاتِ الداكن/السبيا.
4. **الفوتر يُرى**: تباينٌ كافٍ، والفعّالُ بلونٍ **ووزن**.
5. **`Reader.tsx:773`**: «3. آل عمران» ⇒ رقمٌ عربيٌّ بـ`num()`.
6. **رأسان لا يتكرّر فيهما عنصر** وينسحبان معًا بالانزلاق القائم.

**من ج٧ (الأثاث):**
7. **علامةُ الحزب**: يزول القرصُ (`.mp-mark span` — `999px` + خلفيّة) ⇒ الرمزُ المتوارثُ ۞/۩ عاريًا بحجم السطر، وبيانُ «الحزب ٦» بخطٍّ دقيقٍ في الهامش.
8. **سطرُ الهامش**: يزول `border-bottom` ⇒ هامشٌ هادئٌ بلا خطّ.
9. **فاصلُ الصفحات**: يزول الخطُّ الأفقيّ ⇒ رقمُ الصفحة في زخرفٍ صغيرٍ مركزيٍّ والفراغُ يفصل. **لا `<hr>` في صفحة مصحف.**
10. **لوحةُ السورة**: إطارٌ مزخرفٌ رفيعٌ بعرض سطر النصّ، **بلا خلفيّةٍ مصمتةٍ ولا زوايا** — الاسمُ بخطّ المصحف والبياناتُ أدقُّ وأخفت.

**قيود**: لا تُبتكَر هويّةٌ (الأخضرُ والذهبُ والهلالُ كما هي — التنظيمُ لا التبديل) · لونُ التظليل لا يُمَسّ · تباينُ نصّ المصحف ≥٧:١ لا يُكسر · ولا يُمَسّ `lib/sawt/` ولا سطحُ التتبّع.

## الفحصُ عليك (ساكنٌ سريع)

`tsc --noEmit` · **وسطرُ قياسٍ واحدٌ يُلحق بـ`check-wajh.mjs` وآخرُ بـ`check-hayat-mushaf.mjs`** إن لزم لبندَي التوحيد والأقراص (الضبطُ السالب: زرٌّ مخالفٌ وقرصٌ مزروعٌ يُصطادان) — **ولا تُشغّل البوّاباتِ الحيّةَ نفسَها**؛ الإدارةُ تشغّلها عند المراجعة وتُلحق اللقطات.

## العودة

`findings/J67-RESULTS.md`: **جدولٌ واحد** — البندُ · الموضعُ · ما فُعل · وما اجتهدتَ فيه سطرًا. **ولا دفع، ولا لقطات منك** (الإدارةُ تلتقطها مع تشغيل البوّابات).
