/// <reference types="vite/client" />

/** تجزئةُ البيانات التي يحقنها البناءُ (`define` في vite.config) — يعرّفها كلُّ
 *  تطبيقٍ مستهلكٍ في `vite-env.d.ts` عنده، وههنا لتصحّ مراجعةُ الحزمة وحدَها. */
declare const __DATA_VERSION__: string;
