/**
 * **مأخذُ التلاوة من الحزمة المشتركة.**
 *
 * نصُّ المصحف وفهرسُه مادّةٌ واحدةٌ بين «التلاوة» و«مشكاة»، ومصدرُها
 * `@mishkat/quran-core` لا نسخةٌ ههنا. وليس في الهيكل اليومَ إلّا فهرسُ
 * المصحف — يدخل منه القارئُ والتتبّعُ في ف٢ وف٣.
 */
export { AYAH_COUNTS, LAST_AYAH, globalIdOf, locationOf, pad3 } from "@mishkat/quran-core";
